### 1.

Question 1

Which of the following best describes creating a derived class in C#?

[ ]Use the class keyword followed by the name of the base class

[ ]Define a class with a constructor that calls the base class's constructor

[ ]Declare the class with the new keyword followed by the class name

[ ]Use an access modifier followed by the class name and a colon, then the base class name


### 2.

Question 2

How do developers use polymorphism through method overriding in C#?

[ ]By creating a virtual method in the base class and overriding it in the derived classes

[ ]By defining static methods in the base class

[ ]By implementing multiple interfaces in a single class

[ ]By creating multiple constructors in the base class



### 3.

Question 3

Which scenario demonstrates both inheritance and polymorphism in a C# program?

[ ]A class Vehicle has multiple constructors, each with different parameters.

[ ]A class Shape implements two interfaces, IDrawable and IMovable.

[ ]A base class Instrument has a virtual method Play(), and a derived class Piano overrides the Play() method to provide a specific implementation.

[ ]A base class Animal contains only private members that cannot be accessed by derived classes.


### 1. Soru 1

C#’ta türetilmiş bir sınıf oluşturmayı en iyi hangisi açıklar?

* [ ] Use the class keyword followed by the name of the base class
* [ ] Define a class with a constructor that calls the base class's constructor
* [ ] Declare the class with the new keyword followed by the class name
* [X] Use an access modifier followed by the class name and a colon, then the base class name

---

### 2. Soru 2

Geliştiriciler C#’ta metot geçersiz kılma ( *method overriding* ) yoluyla çok biçimliliği nasıl kullanır?

* [X] By creating a virtual method in the base class and overriding it in the derived classes
* [ ] By defining static methods in the base class
* [ ] By implementing multiple interfaces in a single class
* [ ] By creating multiple constructors in the base class

---

### 3. Soru 3

Hangi senaryo bir C# programında hem kalıtımı hem de çok biçimliliği gösterir?

* [ ] A class Vehicle has multiple constructors, each with different parameters.
* [ ] A class Shape implements two interfaces, IDrawable and IMovable.
* [X] A base class Instrument has a virtual method Play(), and a derived class Piano overrides the Play() method to provide a specific implementation.
* [ ] A base class Animal contains only private members that cannot be accessed by derived classes.
