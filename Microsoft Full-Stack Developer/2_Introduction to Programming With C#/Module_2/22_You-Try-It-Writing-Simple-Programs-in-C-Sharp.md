## 🧑‍💻 C# ile Basit Programlar Yazma

### 🧮 Adım 1: Basit Bir Hesap Makinesi Oluşturma

Toplama gibi temel aritmetik işlemleri gerçekleştiren basit bir hesap makinesi programı oluşturun. Bu alıştırma, bir sınıf tanımlamayı ve iki sayıyı toplayan bir metot yazmayı içerir.

**Talimatlar:**

* `Calculator` adlı bir sınıf tanımlayın.
* Sınıfın içinde, sayıları tutmak için iki adet `int` değişken tanımlayın.
* İki sayıyı toplayan ve sonucu döndüren `Add` adlı bir metot yazın.

---

### ▶️ Adım 2: Hesap Makinesi Programını Çalıştırma

Hesap makinesi programını çalıştırmak için bir `Main` metodu yazın. Bu metot, sayılara değer atayacak ve `Add` metodunu çağıracaktır.

**Talimatlar:**

* `Main` metodunu `Calculator` sınıfının içine yazın.
* `number1` ve `number2` değişkenlerine değer atayın.
* `Add` metodunu çağırın ve sonucu `Console.WriteLine` kullanarak yazdırın.

Cevabınızı kontrol etmek için kursun başında oluşturduğunuz **Visual Studio Code** konsol uygulamasını kullanın. Kodunuzu o uygulamadaki `Program.cs` dosyasına yapıştırın (önce mevcut kodları sildiğinizden emin olun). Kodu çalıştırdığınızda hata alırsanız, bir sonraki sayfadaki okumaya giderek kodunuzu doğru cevapla karşılaştırın.

---

## 🔁 Adım 3: Sayıları Görüntülemek İçin Döngü Oluşturma

`1`’den `10`’a kadar sayıları görüntülemek için bir döngü kullanan bir program yazın. Döngüler, eylemleri birden çok kez tekrar etmenizi sağlar.

**Talimatlar:**

* `NumberDisplay` adlı bir sınıf tanımlayın.
* `1`’den `10`’a kadar sayıları yazdırmak için `for` döngüsü kullanan `DisplayNumbers` adlı bir metot yazın.
* `Main` metodunun içinde `DisplayNumbers` metodunu çağırın.

Cevabınızı kontrol etmek için kursun başında oluşturduğunuz **Visual Studio Code** konsol uygulamasını kullanın. Kodunuzu o uygulamadaki `Program.cs` dosyasına yapıştırın (önce mevcut kodları sildiğinizden emin olun). Kodu çalıştırdığınızda hata alırsanız, bir sonraki sayfadaki okumaya giderek kodunuzu doğru cevapla karşılaştırın.

---

## ⌨️ Adım 4: Kullanıcı Girdisi Alma

Kullanıcıyla etkileşim kuran bir program yazın. Bu program kullanıcıdan adını isteyecek ve onu selamlayacaktır.

**Talimatlar:**

* `UserInput` adlı bir sınıf tanımlayın.
* Kullanıcıdan adını girmesini isteyen ve girdiyi bir değişkende saklayan `GreetUser` adlı bir metot yazın.
* Kullanıcının adını içeren bir selamlama mesajı yazdırın.

Cevabınızı kontrol etmek için kursun başında oluşturduğunuz **Visual Studio Code** konsol uygulamasını kullanın. Kodunuzu o uygulamadaki `Program.cs` dosyasına yapıştırın (önce mevcut kodları sildiğinizden emin olun). Kodu çalıştırdığınızda hata alırsanız, bir sonraki sayfadaki okumaya giderek kodunuzu doğru cevapla karşılaştırın.
