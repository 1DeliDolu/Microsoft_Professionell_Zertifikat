### 1.

Question 1

A software engineer is researching frameworks that allow shared code between front-end and back-end components of different application types. They want to avoid rewriting logic for each platform their product will run on. Which .NET feature directly supports this goal?

[ ]Its restriction to Windows-only applications

[ ]Its use only for front-end user interfaces

[ ]Its unified development model for web, desktop, mobile, and cloud applications

[ ]Its dependency on platform-specific code for each type of application

1 point

### 2.

Question 2

While building a .NET application, a developer wants to reduce bugs caused by mixing presentation and data logic in the same files. They’ve heard about “separation of concerns” but are unsure how .NET helps achieve this. What action should they take using the .NET framework?

[ ]Write all application logic inside the Startup class

[ ]Place all code in one file to avoid cross-references

[ ]Use Razor syntax for both data access and front-end rendering

[ ]Create distinct components or projects for UI, business logic, and data handling

1 point

### 3.

Question 3

A developer is having trouble running their .NET application in Visual Studio Code. The code compiles correctly, but breakpoints aren't triggering during debugging.

 What should they investigate first to resolve the issue?

[ ]Whether the Program.cs file contains too many comments

[ ]Whether VS Code is set to dark mode

[ ]Whether the C# extension and launch configuration are properly set up

[ ]Whether the SDK was installed from the Microsoft Store

1 point

### 4.

Question 4

A developer installs the .NET SDK and opens a terminal to run dotnet. Instead of the expected output, they see a message that the command is not recognized. What configuration step did they most likely miss?

[ ]Updating their system’s PATH variable to include the SDK location

[ ]Signing in to a Microsoft account

[ ]Setting a project build target

[ ]Downloading Visual Studio Code

1 point

### 5.

Question 5

A developer just finished writing a small .NET console app and wants to check for any compilation issues before running it. They open the terminal in the project folder. What should they do next to prepare the code for execution?

[ ]Run dotnet restore to start the application

[ ]Run dotnet run to create a project file

[ ]Run dotnet clean to deploy the app

[ ]Run dotnet build to compile the project and detect errors

1 point

### 6.

Question 6

A developer is helping a teammate who can't get their new .NET project to compile. The teammate opened a folder and ran dotnet build, but received an error saying no project or solution file was found. What should the developer conclude is missing?

[ ]The project wasn’t initialized with dotnet new console

[ ]The terminal isn’t in Administrator mode

[ ]The Visual Studio Code theme isn’t properly set

[ ]The .NET CLI is not installed

1 point

### 7.

Question 7

A new developer is troubleshooting a build failure caused by a mismatch in the target framework version and an unresolved dependency. They want to check the file where project settings and package configurations are defined.

Which file should they open to find and correct this information?

[ ]The appsettings.json file

[ ]The Program.cs file

[ ]The launchSettings.json file

[ ]The .csproj file

1 point

### 8.

Question 8

A developer accidentally deleted a file from their .NET project. Now, the application builds correctly but fails to run, showing an error related to the missing entry point. Which file was most likely deleted?

[ ]Program.cs

[ ]launchSettings.json

[ ]README.md

[ ]appsettings.json

1 point

### 9.

Question 9

A developer wants to update the list of NuGet packages and set the application’s target framework. They’re unsure where these settings are stored.

What file should they modify?

[ ]The .csproj file

[ ]launch.json

[ ]Program.cs

[ ]appsettings.json

1 point

### 10.

Question 10

A developer is reorganizing their .NET project files to follow their team’s folder structure. Before moving Program.cs into a subfolder, they want to make sure they understand its purpose. Why is it important to keep Program.cs in the root directory?

[ ]Because it contains the Main method that serves as the application’s entry point

[ ]Because it defines file permissions for the build output

[ ]Because it controls web request routing in ASP.NET apps

[ ]Because it stores NuGet dependencies used during deployment

1 point



### 🧩 Soru 1

**Farklı uygulama türlerinin front-end ve back-end bileşenleri arasında paylaşılan kod kullanımını destekleyip her platform için mantığı yeniden yazmaktan kaçınmayı hedefleyen .NET özelliği hangisidir?**

* [ ] Yalnızca Windows uygulamalarıyla sınırlı olması
* [ ] Sadece front-end kullanıcı arayüzleri için kullanılması
* [X] Web, masaüstü, mobil ve bulut uygulamaları için birleşik geliştirme modeli
* [ ] Her uygulama türü için platforma özgü koda bağımlı olması

---

### 🧩 Soru 2

**Sunum ve veri mantığını aynı dosyalarda karıştırmaktan kaynaklanan hataları azaltmak isteyen bir geliştirici “separation of concerns” yaklaşımını .NET ile nasıl uygulamalıdır?**

* [ ] Tüm uygulama mantığını Startup sınıfının içine yazmak
* [ ] Çapraz referanslardan kaçınmak için tüm kodu tek dosyaya koymak
* [ ] Veri erişimi ve front-end render için Razor syntax’ı birlikte kullanmak
* [X] UI, iş mantığı ve veri işleme için ayrı bileşenler veya projeler oluşturmak

---

### 🧩 Soru 3

**VS Code’da .NET uygulaması derleniyor ama debug sırasında breakpoint’ler tetiklenmiyor. İlk olarak neyi kontrol etmelidir?**

* [ ] `Program.cs` dosyasında çok fazla yorum olup olmadığını
* [ ] VS Code’un koyu modda olup olmadığını
* [X] C# eklentisinin ve launch configuration’ın doğru kurulup kurulmadığını
* [ ] SDK’nın Microsoft Store’dan yüklenip yüklenmediğini

---

### 🧩 Soru 4

**.NET SDK kurulduktan sonra terminalde `dotnet` çalıştırıldığında komut tanınmıyor hatası alınıyor. Büyük olasılıkla hangi yapılandırma adımı atlandı?**

* [X] Sistem PATH değişkenini SDK konumunu içerecek şekilde güncellemek
* [ ] Microsoft hesabıyla oturum açmak
* [ ] Proje build target’ını ayarlamak
* [ ] Visual Studio Code’u indirmek

---

### 🧩 Soru 5

**Küçük bir .NET console app yazıldı ve çalıştırmadan önce derleme hatası var mı kontrol edilmek isteniyor. Proje klasöründeki terminalde sıradaki adım ne olmalıdır?**

* [ ] Uygulamayı başlatmak için `dotnet restore` çalıştırmak
* [ ] Proje dosyası oluşturmak için `dotnet run` çalıştırmak
* [ ] Uygulamayı deploy etmek için `dotnet clean` çalıştırmak
* [X] Projeyi derleyip hataları yakalamak için `dotnet build` çalıştırmak

---

### 🧩 Soru 6

**Bir ekip arkadaşı `dotnet build` çalıştırıyor ama “no project or solution file was found” hatası alıyor. Ne eksik olabilir?**

* [X] Projenin `dotnet new console` ile initialize edilmemiş olması
* [ ] Terminalin Administrator modunda olmaması
* [ ] VS Code temasının doğru ayarlanmaması
* [ ] .NET CLI’nin yüklü olmaması

---

### 🧩 Soru 7

**Target framework uyumsuzluğu ve çözülemeyen dependency nedeniyle build hatası var. Proje ayarları ve paket yapılandırmalarının tanımlandığı dosyayı kontrol etmek istiyor. Hangi dosya açılmalı?**

* [ ] `appsettings.json`
* [ ] `Program.cs`
* [ ] `launchSettings.json`
* [X] `.csproj`

---

### 🧩 Soru 8

**Bir dosya yanlışlıkla silindi. Uygulama build ediyor ama çalıştırırken entry point eksik hatası veriyor. Büyük olasılıkla hangi dosya silinmiştir?**

* [X] `Program.cs`
* [ ] `launchSettings.json`
* [ ] `README.md`
* [ ] `appsettings.json`

---

### 🧩 Soru 9

**NuGet paket listesini güncellemek ve uygulamanın target framework’ünü ayarlamak istiyor. Bu ayarlar nerede tutulur?**

* [X] `.csproj` dosyasında
* [ ] `launch.json`
* [ ] `Program.cs`
* [ ] `appsettings.json`

---

### 🧩 Soru 10

**`Program.cs` dosyasını bir alt klasöre taşımadan önce amacını netleştirmek istiyor. `Program.cs`’yi kök dizinde tutmak neden önemlidir?**

* [X] Uygulamanın giriş noktası olan `Main` metodunu içerdiği için
* [ ] Build çıktısı için dosya izinlerini tanımladığı için
* [ ] ASP.NET uygulamalarında web istek yönlendirmesini kontrol ettiği için
* [ ] Dağıtım sırasında kullanılan NuGet bağımlılıklarını sakladığı için
