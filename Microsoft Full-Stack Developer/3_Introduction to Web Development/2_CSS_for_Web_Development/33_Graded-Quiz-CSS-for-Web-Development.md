
### 1.

Question 1

Which of the following statements is true regarding function declarations in JavaScript?

[ ]Function declarations cannot be called before they are defined

[ ]Function declarations can be called before they are defined

[ ]Function declarations cannot return a value

[ ]Function declarations must be defined inside a block

1 point

### 2.

Question 2

How does JavaScript handle variables declared with var inside a function but outside a block?

[ ]The variable is block-scoped and cannot be accessed outside the block

[ ]The variable is inaccessible outside the function due to block scope

[ ]The variable is hoisted to the top of the block

[ ]The variable is accessible anywhere inside the function due to function scope

1 point

### 3.

Question 3

Which method would you use to change the text of an HTML element with a specific ID?

[ ]document.querySelector('#header').innerHTML = 'New text'

[ ]document.setAttribute('header', 'New text')

[ ]document.createElement('header').textContent = 'New text'

[ ]document.getElementById('header').textContent = 'New text'

1 point

### 4.

Question 4

What is the role of the async keyword in JavaScript?

[ ]It ensures that the function runs faster

[ ]It converts all functions inside it to asynchronous functions

[ ]It declares a function to be asynchronous and allows the use of await

[ ]It creates a callback-free function

1 point

### 5.

Question 5

Which method is commonly used to fetch and parse JSON data from a server in JavaScript?

[ ]document.createElement()

[ ]JSON.parse()

[ ]document.querySelector()

[ ]fetch()

1 point

### 6.

Question 6

What happens if an error occurs in an async function without a try...catch block?

[ ]The error will be ignored completely

[ ]The function will stop executing but will not throw any error

[ ]The error will cause the function to reject its promise, which should be handled by a .catch() method

[ ]The function will restart when it hits the error

1 point

### 7.

Question 7

What happens when you try to access a variable declared with let before its declaration?

[ ]The variable is hoisted and initialized with its assigned value

[ ]The variable returns undefined

[ ]The code throws a ReferenceError because variables declared with let are not initialized before the declaration

[ ]The variable is hoisted and can be used without error

1 point

### 8.

Question 8

Which method is used to attach an event handler to an HTML element?

[ ]addEventListener()

[ ]querySelector()

[ ]setAttribute()

[ ]createElement()

1 point

### 9.

Question 9

Consider the following code:

let greeting = "Hi";

function sayHello() {

  let greeting = "Hello";

  console.log(greeting);

}

sayHello();

console.log(greeting);

What will be printed to the console?

[ ]Hello then Hi

[ ]Hi then Hi

[ ]Hello then Hello

[ ]A referenceerror, because greeting cannot be declared twice in the same script.

1 point

### 10.

Question 10

How can you handle errors when working with promises?

[ ]By using the .catch() method, which allows you to catch any errors that occur during the promise’s execution

[ ]By using the .finally() method to catch errors in promises

[ ]By using the await keyword to automatically handle errors

[ ]By using a try...catch block to catch errors in promise chains

1 point
