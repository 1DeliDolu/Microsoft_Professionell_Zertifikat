### 1.

Question 1

What is the purpose of middleware in an ASP.NET Core application?

[ ]To connect the application directly to the database

[ ]To compile code before the application starts

[ ]To process HTTP requests and responses as part of a configurable pipeline

[ ]To automatically generate UI components for the application


### 2.

Question 2

What does the middleware pipeline in ASP.NET Core do?

[ ]It manages the deployment of ASP.NET Core applications

[ ]It directly connects users to the database for data retrieval

[ ]It processes HTTP requests in a series of steps before returning a response

[ ]It generates static web pages without handling user requests


### 3.

Question 3

What is the role of middleware in an ASP.NET Core application?

[ ]Middleware components handle HTTP requests and responses as part of a configurable request pipeline

[ ]Middleware components compile the code before the application starts

[ ]Middleware components are responsible for connecting to the database in an ASP.NET Core application

[ ]Middleware components automatically generate UI components for the application


### 1.

**Question 1**
What is the purpose of middleware in an ASP.NET Core application?

[ ]To connect the application directly to the database
[ ]To compile code before the application starts
[x]To process HTTP requests and responses as part of a configurable pipeline

Middleware in ASP.NET Core processes HTTP requests and responses as part of a configurable pipeline.
[ ]To automatically generate UI components for the application

---

### 2.

**Question 2**
What does the middleware pipeline in ASP.NET Core do?

[ ]It manages the deployment of ASP.NET Core applications
[ ]It directly connects users to the database for data retrieval
[x]It processes HTTP requests in a series of steps before returning a response

The middleware pipeline processes HTTP requests through a series of middleware components, each handling a specific task like logging or authentication before generating a response.
[ ]It generates static web pages without handling user requests

---

### 3.

**Question 3**
What is the role of middleware in an ASP.NET Core application?

[x]Middleware components handle HTTP requests and responses as part of a configurable request pipeline

Middleware components handle HTTP requests and responses as part of a configurable request pipeline.
[ ]Middleware components compile the code before the application starts
[ ]Middleware components are responsible for connecting to the database in an ASP.NET Core application
[ ]Middleware components automatically generate UI components for the application
