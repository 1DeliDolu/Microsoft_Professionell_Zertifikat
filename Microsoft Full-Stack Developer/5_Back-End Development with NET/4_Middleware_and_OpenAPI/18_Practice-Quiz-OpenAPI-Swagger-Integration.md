### 1.

Question 1

What is one key benefit of using OpenAPI in API development?

[ ]It generates backend code for APIs

[ ]It provides a standardized way to describe APIs and their functionalities

[ ]It automatically secures the API for production use

[ ]It handles API version control


### 2.

Question 2

What is the primary role of Swagger in ASP.NET Core?

[ ]It ensures the API is secure and ready for production use

[ ]It provides an interactive interface for testing and interacting with APIs directly from the browser

[ ]It manages API versioning and updates

[ ]It automatically generates client-side code for the API


### 3.

Question 3

How does integrating Swagger with ASP.NET Core improve the API development process?

[ ]It automatically handles API versioning and deprecates older versions

[ ]It enforces API security by adding authentication mechanisms

[ ]It optimizes API performance by reducing response times

[ ]It automatically generates interactive API documentation that allows developers to test endpoints directly from the browser



### 1.

**Question 1**
What is one key benefit of using OpenAPI in API development?

[ ]It generates backend code for APIs
[x]It provides a standardized way to describe APIs and their functionalities
[ ]It automatically secures the API for production use
[ ]It handles API version control

---

### 2.

**Question 2**
What is the primary role of Swagger in ASP.NET Core?

[ ]It ensures the API is secure and ready for production use
[x]It provides an interactive interface for testing and interacting with APIs directly from the browser
[ ]It manages API versioning and updates
[ ]It automatically generates client-side code for the API

---

### 3.

**Question 3**
How does integrating Swagger with ASP.NET Core improve the API development process?

[ ]It automatically handles API versioning and deprecates older versions
[ ]It enforces API security by adding authentication mechanisms
[ ]It optimizes API performance by reducing response times
[x]It automatically generates interactive API documentation that allows developers to test endpoints directly from the browser
