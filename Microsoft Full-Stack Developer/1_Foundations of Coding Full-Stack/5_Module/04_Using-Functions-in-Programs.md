## 🧩 Using Functions in Programs

Fonksiyonların ne olduğunu ve nasıl oluşturulduklarını anlamak önemlidir, ancak bu yeniden kullanılabilir kod parçaları daha büyük program içinde nasıl işe koşulur?

Bu videoda, program içinde tanımlanmış fonksiyonları kullanacağız. Daha önce tanımlanmış bir fonksiyonu kullanmak için, onu programın içinden çağırmanız gerekir. Bir fonksiyonun çağrılma sürecini adım adım inceleyelim.

---

## 📞 Fonksiyon Çağırma Süreci

İlk olarak, bir fonksiyonu adını yazıp ardından parantez ekleyerek çağırırsınız.

İkinci olarak, fonksiyonun parametreleri varsa, bu parametreleri parantezin içinde sağlarsınız.

Diyelim ki bir alışveriş sepetindeki ürünlerin toplam maliyetini hesaplamak için **"calculateTotal"** adlı bir fonksiyonumuz var.

Bu fonksiyonu çağırmak için, fonksiyon adının yanına açma ve kapama parantezi koyarak **calculate total** yazarsınız. Parantezin içine ise, fonksiyona geçirilen parametre olan **items** kelimesini yazarsınız.

Fonksiyonlar bir programda bu şekilde çağrılır.

---

## ⚙️ Fonksiyonun Çalışma Mantığı

Bir fonksiyonu çağırdığınızda, fonksiyon gövdesinin içindeki kod yürütülür ve belirli görevi gerçekleştirir. Şimdi fonksiyon yürütümünün nasıl çalıştığını inceleyelim.

İlk olarak, program kontrolü fonksiyon gövdesine girer.

İkinci olarak, fonksiyonun içindeki kod satır satır yürütülür.

Üçüncü olarak, eğer fonksiyonun bir **return** ifadesi varsa, çağırana bir değer döndürür.

**Return** ifadesi, fonksiyonun döndürdüğü değeri belirtir. Dönüş değerini bir değişkende saklayabilir veya doğrudan kullanabilirsiniz.

---

## 🎯 Özet

Bu videoda, bir program içinde tanımlanmış fonksiyonlarla nasıl çalışılacağını öğrendiniz. Fonksiyonların nasıl çağrıldığını ve yürütülmesini, ayrıca dönüş değerlerini nasıl anlamanız gerektiğini inceledik.
