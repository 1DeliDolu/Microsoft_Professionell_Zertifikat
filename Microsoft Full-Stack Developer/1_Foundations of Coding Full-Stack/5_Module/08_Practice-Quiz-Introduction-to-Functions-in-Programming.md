### 1.

Question 1

What is one key advantage of using functions in programming?

[ ]Functions help developers create reusable and modular code blocks

[ ]Functions eliminate the need for control structures like loops

[ ]Functions are only useful for graphical interfaces

[ ]Functions automatically detect and fix errors in code

1 point

### 2.

Question 2

A developer needs to write a function to calculate the square of a number. Which of the following correctly declares this function in pseudocode?

[ ]function square() { number * number; }

[ ]function square(number) { return number * number; }

[ ]function { return number * number; }

[ ]square = { number * number; }

1 point

### 3.

Question 3

A function calculateTotal(items) processes a list of prices and returns their total. What happens when you call calculateTotal([10, 20, 30])?

[ ]The function processes the list [10, 20, 30] and returns 60 as the total

[ ]The function modifies the list [10, 20, 30] directly in memory

[ ]The function stores the list [10, 20, 30] as a variable and outputs it

[ ]The function doesn’t execute because it requires no parameters

1 point

### 4.

Question 4

You need to write a function to calculate the area of a rectangle. Which of the following correctly defines this function?

[ ]function calculateArea(length) { return length * width; }

[ ]function calculateArea() { length * width; }

[ ]function calculateArea(length, width) { return length * width; }

[ ]calculateArea(length, width) = length * width;

1 point

### 5.

Question 5

How do parameters enhance the reusability of functions?

[ ]Parameters allow functions to process different inputs and produce flexible outputs

[ ]Parameters ensure the function always executes faster

[ ]Parameters automatically handle error detection in the function

[ ]Parameters remove the need for return statements

1 point


### 1.

**Question 1**

Programlamada fonksiyon kullanmanın temel avantajlarından biri nedir?

* [X] Fonksiyonlar, geliştiricilerin yeniden kullanılabilir ve modüler kod blokları oluşturmasına yardımcı olur
* [ ] Fonksiyonlar, döngüler gibi kontrol yapılarına olan ihtiyacı ortadan kaldırır
* [ ] Fonksiyonlar yalnızca grafik arayüzler için kullanışlıdır
* [ ] Fonksiyonlar kod hatalarını otomatik olarak algılar ve düzeltir

---

### 2.

**Question 2**

Bir geliştiricinin bir sayının karesini hesaplayan bir fonksiyon yazması gerekiyor. Aşağıdakilerden hangisi bu fonksiyonu *pseudocode* ile doğru şekilde bildirir?

* [ ] function square() { number * number; }
* [X] function square(number) { return number * number; }
* [ ] function { return number * number; }
* [ ] square = { number * number; }

---

### 3.

**Question 3**

`calculateTotal(items)` fonksiyonu fiyat listesini işler ve toplamını döndürür. `calculateTotal([10, 20, 30])` çağrıldığında ne olur?

* [X] Fonksiyon `[10, 20, 30]` listesini işler ve toplam olarak **60** döndürür
* [ ] Fonksiyon `[10, 20, 30]` listesini bellekte doğrudan değiştirir
* [ ] Fonksiyon `[10, 20, 30]` listesini bir değişkende saklar ve çıktılar
* [ ] Fonksiyon çalışmaz çünkü parametre gerektirmez

---

### 4.

**Question 4**

Bir dikdörtgenin alanını hesaplamak için bir fonksiyon yazmanız gerekiyor. Aşağıdakilerden hangisi bu fonksiyonu doğru şekilde tanımlar?

* [ ] function calculateArea(length) { return length * width; }
* [ ] function calculateArea() { length * width; }
* [X] function calculateArea(length, width) { return length * width; }
* [ ] calculateArea(length, width) = length * width;

---

### 5.

**Question 5**

Parametreler fonksiyonların yeniden kullanılabilirliğini nasıl artırır?

* [X] Parametreler, fonksiyonların farklı girdileri işlemesini ve esnek çıktılar üretmesini sağlar
* [ ] Parametreler fonksiyonun her zaman daha hızlı çalışmasını sağlar
* [ ] Parametreler fonksiyon içinde hata tespitini otomatik yapar
* [ ] Parametreler dönüş (return) ifadelerine olan ihtiyacı ortadan kaldırır
